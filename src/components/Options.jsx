import { Button, TouchableOpacity } from "react-native";

export const Options = ({ openDrawer }) => {
  return (
    <TouchableOpacity>
      <Button title="|||" onPress={() => null} />
    </TouchableOpacity>
  );
};
